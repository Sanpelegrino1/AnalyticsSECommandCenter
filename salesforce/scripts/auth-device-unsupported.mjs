console.error(
  "Device login is not currently exposed in the documented modern Salesforce CLI org-login commands used by this workspace. Use 'npm run auth:web', '.\\scripts\\auth-web.ps1', 'sf org login sfdx-url', or 'sf org login jwt' instead."
);
process.exit(1);