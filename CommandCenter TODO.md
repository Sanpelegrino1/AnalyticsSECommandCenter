# CommandCenter TODO

## System Prompt
```text
You are one of several implementation agents working in the AnalyticsSECommandCenter repository.

This is a shared workspace. Before you edit anything, read this TODO file, check the Agent Message Board, and claim your scope with a new entry.

Work only on the prompt or phase assigned to you. Do not edit files owned by another active agent. If you discover a dependency, blocker, or overlap, post it to the Agent Message Board instead of branching into adjacent work.

Use the smallest safe change set. Prefer existing scripts, playbooks, registries, and metadata over inventing new patterns.

When you finish a milestone, post a short handoff note with the files changed, validation performed, and any follow-up required.
```

## Working Rules

- The prompts below are ordered in the sequence I recommend executing them.
- Each prompt below is a single-agent task. Do not run two prompts from this file at the same time.
- If a prompt depends on a prior prompt, wait for the prior prompt to finish and record the handoff in the Agent Message Board.
- Treat this file as the coordination source of truth for the current implementation run.

## Follow-up Backlog

- Learn and document how to create a Tableau Next workspace programmatically or through a repeatable operator workflow, then add a repo-native wrapper or playbook for workspace creation. Current evidence: publishing to `AgentServer` was blocked until a `Demo` workspace was created manually in the org.

## Verified Current State

These are the main implementation surfaces I confirmed in the repo before writing the prompts:

- `scripts/salesforce/setup-command-center-connected-app.ps1` deploys the repo-owned `CommandCenterAuth` external client app.
- `scripts/salesforce/data-cloud-login-web.ps1` performs browser auth for Data Cloud and stores local-only auth values.
- `scripts/salesforce/data-cloud-get-access-token.ps1` resolves a usable Data Cloud access context without printing secrets.
- `scripts/salesforce/data-cloud-register-target.ps1` and `scripts/salesforce/data-cloud-register-manifest-targets.ps1` persist non-secret target metadata.
- `scripts/salesforce/data-cloud-guided-manifest-setup.ps1` wires manifest-driven target registration and warns that objectEndpoint still has to match the live connector.
- `scripts/salesforce/data-cloud-generate-schema.ps1` infers field types from CSV samples and writes a schema draft.
- `scripts/salesforce/data-cloud-generate-ingest-metadata.ps1` generates connector, source, object, stream-definition, and object-source-target-map metadata from a manifest.
- `scripts/salesforce/data-cloud-create-manifest-streams.ps1` can register schema objects and create Data Cloud streams in the org.
- `scripts/salesforce/data-cloud-upload-csv.ps1` and `scripts/salesforce/data-cloud-upload-manifest.ps1` handle upload execution.
- `notes/registries/data-cloud-targets.json` is the target registry and already contains Sunrun-specific state, including a stale-org collision remap.
- `notes/registries/salesforce-orgs.json` shows the separate normal org alias and Data Cloud alias split.
- `notes/evaluations/tableau-next-claude-skills.md` is evaluation-only reference material, not an implemented Tableau Next workflow.

## Agent Message Board

Use this board for coordination between agents. Append only. Do not rewrite another agent's message.

| UTC | Agent | Phase | Status | Message | Files / Blockers |
| --- | --- | --- | --- | --- | --- |
|  |  |  | claimed |  |  |
| 2026-04-20T11:50:00Z | GitHub Copilot | Zero-touch orchestration | in progress | Claimed the cross-phase authenticated-to-SDM automation pass. Scope: one orchestration entry point, shared preflight and readiness reporting, discovery-driven Tableau Next target resolution, direct semantic-model inspection, and documentation for zero-touch-after-authentication boundaries only. | [scripts/salesforce/setup-command-center-connected-app.ps1](scripts/salesforce/setup-command-center-connected-app.ps1); [scripts/salesforce/data-cloud-guided-manifest-setup.ps1](scripts/salesforce/data-cloud-guided-manifest-setup.ps1); [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1); [scripts/tableau/register-next-target.ps1](scripts/tableau/register-next-target.ps1); [scripts/tableau/inspect-next-target.ps1](scripts/tableau/inspect-next-target.ps1); [scripts/tableau/upsert-next-semantic-model.ps1](scripts/tableau/upsert-next-semantic-model.ps1); [README.md](README.md); [playbooks/prepare-tableau-next-semantic-model.md](playbooks/prepare-tableau-next-semantic-model.md); [CommandCenter TODO.md](CommandCenter%20TODO.md) |
| 2026-04-20T02:05:00Z | GitHub Copilot | Phase 5 audit | in progress | Claimed the follow-up audit of the prior upload retry and recovery work. Scope is current upload control flow, stale objectEndpoint fallback safety, operator inspect or abort guidance, and the matching docs. | scripts/salesforce/DataCloud.Common.ps1; scripts/salesforce/data-cloud-upload-csv.ps1; scripts/salesforce/data-cloud-upload-manifest.ps1; scripts/salesforce/data-cloud-list-jobs.ps1; scripts/salesforce/data-cloud-get-job.ps1; scripts/salesforce/data-cloud-abort-job.ps1; playbooks/data-cloud-upload-csv.md; playbooks/upload-manifest-dataset-to-data-cloud.md; skills/upload-csv-to-data-cloud.md |
| 2026-04-20T10:15:00Z | GitHub Copilot | Validation | in progress | Claimed the smallest end-to-end Data Cloud workflow validation run: CommandCenterAuth deployment, Data Cloud auth resolution, manifest target registration, single-table stream provisioning, and upload execution state capture. | [scripts/salesforce/setup-command-center-connected-app.ps1](scripts/salesforce/setup-command-center-connected-app.ps1); [scripts/salesforce/data-cloud-get-access-token.ps1](scripts/salesforce/data-cloud-get-access-token.ps1); [scripts/salesforce/data-cloud-register-manifest-targets.ps1](scripts/salesforce/data-cloud-register-manifest-targets.ps1); [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1); [scripts/salesforce/data-cloud-upload-manifest.ps1](scripts/salesforce/data-cloud-upload-manifest.ps1) |
| 2026-04-20T02:00:15Z | GitHub Copilot | Validation | completed | Current re-run evidence, smallest realistic scope: `setup-command-center-connected-app.ps1 -TargetOrg STORM_TABLEAU_NEXT` deployed `CommandCenterAuth` successfully as job `0AfKZ0000068DyI0AU`; `data-cloud-get-access-token.ps1 -TargetKey sunrun-projects -AsJson` resolved Data Cloud auth with `tokenSource` `salesforce-token-exchange` and dedicated alias `STORM_TABLEAU_NEXT_DC`; and `data-cloud-register-manifest-targets.ps1 -ManifestPath Demos/Sunrun Demo/manifest.json -TargetKeyPrefix sunrun` refreshed the six `sunrun-*` registry rows. Live-org failures remain: `data-cloud-create-manifest-streams.ps1` for table `projects` reused schema object `sunrun2_projects` but failed with `400 BAD_REQUEST` because EDTO `sunrun2_projects` is already in use, and `data-cloud-upload-csv.ps1 -TargetKey sunrun-projects -CsvPath tmp/phase8-projects-sample.csv` failed at job creation with `409 CONFLICT` on `/api/v1/ingest/jobs` after warning that `sunrun-projects` has no saved `objectEndpoint` and had to fall back to generic lookup. Remaining org-specific: Setup visibility and OAuth policy for the deployed external client app, connector and stream uniqueness in Data Cloud, connector-specific `objectEndpoint` capture in the target registry, and whatever live ingest state is causing the current `409 CONFLICT` during generic job creation. | [scripts/salesforce/setup-command-center-connected-app.ps1](scripts/salesforce/setup-command-center-connected-app.ps1); [scripts/salesforce/data-cloud-get-access-token.ps1](scripts/salesforce/data-cloud-get-access-token.ps1); [scripts/salesforce/data-cloud-register-manifest-targets.ps1](scripts/salesforce/data-cloud-register-manifest-targets.ps1); [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1) failed against live stream state `EDTO 'sunrun2_projects' already in use`; [scripts/salesforce/data-cloud-upload-csv.ps1](scripts/salesforce/data-cloud-upload-csv.ps1) failed against live ingest state `409 CONFLICT` with no saved `objectEndpoint` for `sunrun-projects` |
| 2026-04-20T01:59:17Z | GitHub Copilot | Validation | completed | Re-ran the smallest realistic end-to-end path after fixing CommandCenterAuth metadata path literals in `setup-command-center-connected-app.ps1`. Succeeded: CommandCenterAuth redeployed to `STORM_TABLEAU_NEXT` with deployment job `0AfKZ0000068DyD0AU`; `data-cloud-get-access-token.ps1 -TargetKey sunrun-projects -AsJson` resolved auth for alias `STORM_TABLEAU_NEXT_DC`; and `data-cloud-register-manifest-targets.ps1` refreshed the `sunrun-*` rows from the manifest. Failed on live org state, not manifest-specific hardcoding: `data-cloud-create-manifest-streams.ps1` returned `400 BAD_REQUEST` with `Unable to create a data-stream. EDTO 'sunrun2_projects' already in use. Connector name: sunrun2_projects`, and `data-cloud-upload-csv.ps1` returned `409 CONFLICT` because ingest job `1c251a01-54c0-41e4-9a5e-d9a0e2e26b01` for object `sunrun2_projects` was still `InProgress`. Remaining org-specific: External Client App visibility and OAuth policy in Setup, Ingestion API connector and stream uniqueness, saved `objectEndpoint` capture for target rows, and clearing or waiting active ingest jobs before retry. | [scripts/salesforce/setup-command-center-connected-app.ps1](scripts/salesforce/setup-command-center-connected-app.ps1); [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1); [scripts/salesforce/data-cloud-upload-csv.ps1](scripts/salesforce/data-cloud-upload-csv.ps1) |
| 2026-04-20T01:55:47Z | GitHub Copilot | Validation | completed | Smoke test result: Data Cloud auth resolved for `sunrun-projects` via `salesforce-token-exchange`; manifest target registration refreshed the Sunrun dataset rows; and a 10-row upload sample reached `JobComplete` as job `23da6ad7-1db5-455e-91c8-22076a3da578` with an inspect command emitted by the hardened upload path. Failures: `setup-command-center-connected-app.ps1` stopped before deploy because it could not find `salesforce/force-app/main/default/extlClntAppGlobalOauthSets/CommandCenterAuth_glbloauth.ecago-meta.xml`, and `data-cloud-create-manifest-streams.ps1` hit `400 BAD_REQUEST` with `Unable to create a data-stream. EDTO 'sunrun2_projects' already in use.` while trying to create or resolve the `projects` stream. Remaining org-specific conditions: the preferred CLI-session alias `STORM_TABLEAU_NEXT_DC` is not currently authorized on this machine, the connector and stream must already exist in Data Cloud, and this validation still used generic object lookup because `sunrun-projects` has no saved `objectEndpoint`. | [scripts/salesforce/setup-command-center-connected-app.ps1](scripts/salesforce/setup-command-center-connected-app.ps1); [scripts/salesforce/data-cloud-get-access-token.ps1](scripts/salesforce/data-cloud-get-access-token.ps1); [scripts/salesforce/data-cloud-register-manifest-targets.ps1](scripts/salesforce/data-cloud-register-manifest-targets.ps1); [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1); [scripts/salesforce/data-cloud-upload-csv.ps1](scripts/salesforce/data-cloud-upload-csv.ps1) |
| 2026-04-20T01:45:00Z | GitHub Copilot | Docs follow-up | in progress | Claimed the documentation and skill refresh for newly automated Data Cloud manifest provisioning. Scope is README, Data Cloud playbooks, and skill discoverability only. | README.md; playbooks/guided-data-cloud-manifest-setup.md; playbooks/set-up-command-center-connected-app.md; playbooks/upload-manifest-dataset-to-data-cloud.md; skills/INDEX.md; skills/guided-data-cloud-manifest-setup.md |
| 2026-04-20T09:05:00Z | GitHub Copilot | Phase 2 | in progress | Claimed manifest target-registration normalization. Scope is deterministic manifest-derived registry fields, preservation of org-only target metadata, and preflight diagnostics for stale rows before upload. | [scripts/salesforce/DataCloud.Common.ps1](scripts/salesforce/DataCloud.Common.ps1), [scripts/salesforce/data-cloud-register-target.ps1](scripts/salesforce/data-cloud-register-target.ps1), [scripts/salesforce/data-cloud-register-manifest-targets.ps1](scripts/salesforce/data-cloud-register-manifest-targets.ps1), [scripts/salesforce/data-cloud-guided-manifest-setup.ps1](scripts/salesforce/data-cloud-guided-manifest-setup.ps1), [scripts/salesforce/data-cloud-upload-manifest.ps1](scripts/salesforce/data-cloud-upload-manifest.ps1), [playbooks/guided-data-cloud-manifest-setup.md](playbooks/guided-data-cloud-manifest-setup.md), [playbooks/upload-manifest-dataset-to-data-cloud.md](playbooks/upload-manifest-dataset-to-data-cloud.md) |
| 2026-04-20T09:25:00Z | GitHub Copilot | Phase 2 | completed | Manifest target registration now derives stable dataset metadata from the manifest, keeps registry paths workspace-relative, preserves org-only values like `salesforceAlias`, `tenantEndpoint`, and `objectEndpoint` on re-registration, and blocks manifest upload with explicit stale-row diagnostics plus a repair command. Remaining manual Data Cloud Setup: the org still has to own the live Ingestion API connector, data streams, and connector-specific `objectEndpoint` values. Validation: VS Code error scan plus PowerShell parse on the edited scripts. | [scripts/salesforce/DataCloud.Common.ps1](scripts/salesforce/DataCloud.Common.ps1), [scripts/salesforce/data-cloud-register-target.ps1](scripts/salesforce/data-cloud-register-target.ps1), [scripts/salesforce/data-cloud-register-manifest-targets.ps1](scripts/salesforce/data-cloud-register-manifest-targets.ps1), [scripts/salesforce/data-cloud-guided-manifest-setup.ps1](scripts/salesforce/data-cloud-guided-manifest-setup.ps1), [scripts/salesforce/data-cloud-upload-manifest.ps1](scripts/salesforce/data-cloud-upload-manifest.ps1), [playbooks/guided-data-cloud-manifest-setup.md](playbooks/guided-data-cloud-manifest-setup.md), [playbooks/upload-manifest-dataset-to-data-cloud.md](playbooks/upload-manifest-dataset-to-data-cloud.md) |
| 2026-04-20T08:34:45Z | GitHub Copilot | Phase 5 | completed | Hardened Data Cloud upload retries and recovery. CSV uploads now fall back from stale objectEndpoint metadata to generic lookup, annotate jobs with inspect or abort commands, and best-effort abort unfinished jobs when upload or close fails. Added an explicit abort helper and updated operator docs. Remaining limits: this repo can abort stuck jobs and surface stale-target guidance, but it cannot recreate missing Data Cloud streams or repair field mappings automatically. | scripts/salesforce/DataCloud.Common.ps1; scripts/salesforce/data-cloud-upload-csv.ps1; scripts/salesforce/data-cloud-upload-manifest.ps1; scripts/salesforce/data-cloud-list-jobs.ps1; scripts/salesforce/data-cloud-get-job.ps1; scripts/salesforce/data-cloud-abort-job.ps1; scripts/salesforce/data-cloud-inspect-csv.ps1; README.md; playbooks/data-cloud-upload-csv.md; playbooks/upload-manifest-dataset-to-data-cloud.md; skills/upload-csv-to-data-cloud.md |
| 2026-04-20T01:51:47Z | GitHub Copilot | Docs follow-up | completed | Updated the README, Data Cloud playbooks, and skill surfaces so the repo now states the real automation boundary: connector creation stays manual, while manifest-driven schema registration, stream reconciliation, DLO activation checks, registry sync, and `provisioning-state.json` output are org-automated once auth and the connector exist. Added a dedicated skill for manifest stream provisioning. Remaining doc-only gap: there is still no standalone playbook focused only on manifest stream bootstrap and optional deployment of the generated `salesforce/generated/<sourceName>/` metadata. | README.md; playbooks/guided-data-cloud-manifest-setup.md; playbooks/set-up-command-center-connected-app.md; playbooks/upload-manifest-dataset-to-data-cloud.md; skills/INDEX.md; skills/guided-data-cloud-manifest-setup.md; skills/provision-data-cloud-manifest-streams.md |
| 2026-04-20T00:00:00Z | GitHub Copilot | Phase 4 | in progress | Claimed manifest-driven stream and DLO automation hardening. Narrow scope: live schema and stream reconciliation, registry sync, and operator-state reporting. | scripts/salesforce/data-cloud-create-manifest-streams.ps1; scripts/salesforce/data-cloud-generate-ingest-metadata.ps1 |
| 2026-04-20T01:35:24Z | GitHub Copilot | Phase 4 | completed | Manifest-driven provisioning now reuses compatible schemas, fails fast on schema drift, resolves or creates streams idempotently, waits for ACTIVE DLO status, syncs registry objectName and objectEndpoint to the live accepted object, and writes a provisioning-state report that separates org automation from undeployed local metadata. Remaining manual steps: the initial Ingest API connector still must already exist in Data Cloud, and local metadata deployment remains a separate operator workflow. | scripts/salesforce/data-cloud-create-manifest-streams.ps1; scripts/salesforce/data-cloud-generate-ingest-metadata.ps1; CommandCenter TODO.md |
| 2026-04-20T00:40:00Z | GitHub Copilot | Phase 3 | in progress | Claimed the schema inference and manifest metadata coherence work. Plan is to unify CSV validation and field inference across inspect, schema draft, and manifest metadata generation, then document the generated-path behavior. | [scripts/salesforce/DataCloud.Common.ps1](scripts/salesforce/DataCloud.Common.ps1); [scripts/salesforce/data-cloud-generate-schema.ps1](scripts/salesforce/data-cloud-generate-schema.ps1); [scripts/salesforce/data-cloud-generate-ingest-metadata.ps1](scripts/salesforce/data-cloud-generate-ingest-metadata.ps1); [scripts/salesforce/data-cloud-inspect-csv.ps1](scripts/salesforce/data-cloud-inspect-csv.ps1) |
| 2026-04-20T00:00:00Z | GitHub Copilot | Phase 6 | in progress | Claimed the Tableau Next semantic-model planning work to convert the evaluation into a repo-native starter workflow and roadmap. | [README.md](README.md); [notes/evaluations/tableau-next-claude-skills.md](notes/evaluations/tableau-next-claude-skills.md) |
| 2026-04-20T00:25:00Z | GitHub Copilot | Phase 6 | completed | Added a local Tableau Next semantic-model starter playbook and README status note. Data Cloud stability is now an explicit prerequisite. Missing entirely: Tableau Next target registry, PowerShell discovery and inspection wrappers, setup and teardown automation, and local troubleshooting coverage. | [playbooks/prepare-tableau-next-semantic-model.md](playbooks/prepare-tableau-next-semantic-model.md); [README.md](README.md) |
| 2026-04-20T01:10:00Z | GitHub Copilot | Phase 3 | completed | Unified CSV validation and field inference in `DataCloud.Common.ps1`, so schema draft, CSV inspection, manifest metadata generation, and manifest stream bootstrap now share one path. Schema inference remains an optional standalone review helper and is also part of the bootstrap path through `data-cloud-create-manifest-streams.ps1`. Validated with PowerShell load checks plus Sunrun schema generation, metadata generation, and CSV inspection, including default generated paths under `salesforce/generated/`. | [scripts/salesforce/DataCloud.Common.ps1](scripts/salesforce/DataCloud.Common.ps1); [scripts/salesforce/data-cloud-generate-schema.ps1](scripts/salesforce/data-cloud-generate-schema.ps1); [scripts/salesforce/data-cloud-generate-ingest-metadata.ps1](scripts/salesforce/data-cloud-generate-ingest-metadata.ps1); [scripts/salesforce/data-cloud-inspect-csv.ps1](scripts/salesforce/data-cloud-inspect-csv.ps1); [README.md](README.md); [playbooks/guided-data-cloud-manifest-setup.md](playbooks/guided-data-cloud-manifest-setup.md); [skills/guided-data-cloud-manifest-setup.md](skills/guided-data-cloud-manifest-setup.md) |
| 2026-04-20T02:04:41Z | GitHub Copilot | Phase 6 audit | in progress | Claimed the follow-up audit of the Tableau Next semantic-model starter docs. Scope is to verify the README/playbook boundary against current repo-native entry points, tighten any implied non-existent automation, and report the result. | [playbooks/prepare-tableau-next-semantic-model.md](playbooks/prepare-tableau-next-semantic-model.md); [README.md](README.md); [notes/evaluations/tableau-next-claude-skills.md](notes/evaluations/tableau-next-claude-skills.md); [CommandCenter TODO.md](CommandCenter TODO.md) |
| 2026-04-20T10:32:00Z | GitHub Copilot | Validation | completed | Smallest realistic end-to-end rerun against the Sunrun `projects` table produced reusable workflow evidence with exact failure states. Succeeded: `data-cloud-get-access-token.ps1 -TargetKey sunrun-projects -AsJson` resolved Data Cloud auth through `salesforce-token-exchange` for alias `STORM_TABLEAU_NEXT_DC`; `data-cloud-register-manifest-targets.ps1 -ManifestPath '.\Demos\Sunrun Demo\manifest.json' -TargetKeyPrefix sunrun` refreshed deterministic target rows; the stream provisioner reached the live connector and confirmed schema `sunrun2_projects` already exists on `sunrun_ingest_api`. Failed: `setup-command-center-connected-app.ps1 -TargetOrg STORM_TABLEAU_NEXT` stopped in repo preflight because it expects missing metadata path `salesforce/force-app/main/default/extlClntAppGlobalOauthSets/CommandCenterAuth_glbloauth.ecago-meta.xml` while the repo currently contains `CommandCenterAuth_glbloauth.ecaGlblOauth-meta.xml`; `data-cloud-create-manifest-streams.ps1` then hit live 400 `EDTO 'sunrun2_projects' already in use`, and a direct stream listing showed an ACTIVE existing stream named `sunrun2_projects_sunrun2_projects_A91ABE55` that the resolver did not match; `data-cloud-upload-csv.ps1` hit live 409 conflict on job creation, and `data-cloud-list-jobs.ps1` showed the conflicting state is job `1c251a01-54c0-41e4-9a5e-d9a0e2e26b01` in `InProgress` for object `sunrun2_projects`. Remaining org-specific: the live Data Cloud connector and stream naming conventions still control whether existing streams can be reused, and unfinished ingest jobs in the org still block new uploads until they complete or are aborted. | [CommandCenter TODO.md](CommandCenter%20TODO.md); [scripts/salesforce/setup-command-center-connected-app.ps1](scripts/salesforce/setup-command-center-connected-app.ps1); [salesforce/force-app/main/default/extlClntAppGlobalOauthSets/CommandCenterAuth_glbloauth.ecaGlblOauth-meta.xml](salesforce/force-app/main/default/extlClntAppGlobalOauthSets/CommandCenterAuth_glbloauth.ecaGlblOauth-meta.xml); [scripts/salesforce/data-cloud-get-access-token.ps1](scripts/salesforce/data-cloud-get-access-token.ps1); [scripts/salesforce/data-cloud-register-manifest-targets.ps1](scripts/salesforce/data-cloud-register-manifest-targets.ps1); [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1); [scripts/salesforce/data-cloud-upload-csv.ps1](scripts/salesforce/data-cloud-upload-csv.ps1); [notes/registries/data-cloud-targets.json](notes/registries/data-cloud-targets.json) |
| 2026-04-20T01:57:47Z | GitHub Copilot | Phase 5 audit | completed | Verified the current Phase 5 upload path against the shared helpers, upload scripts, and docs. Result: the original outcome was mostly correct. Retry, inspect, and abort behavior still match the current scripts, and cleanup remains proportionate because the upload path only auto-aborts jobs that were created but never closed, while timeouts after close return inspect and abort commands instead of forcing cancellation. Manifest uploads are better protected than ad hoc per-table uploads because `data-cloud-upload-manifest.ps1` preflights dataset-derived target drift before calling `data-cloud-upload-csv.ps1`. One local defect was fixed during audit: the stale `objectEndpoint` recovery message had fallen behind the implementation and implied operators should manually remove `objectEndpoint` and retry generic lookup even though the script already does that automatically. Stale endpoint fallback improves recovery without adding silent misrouting inside the manifest path because generic lookup still uses the registered `sourceName` plus `objectName`, but it cannot automatically detect a user-selected wrong per-table target outside manifest preflight. Remaining unrecoverable automatically: missing streams, wrong `sourceName` or `objectName`, incompatible field mappings, and other org-side stream or connector defects. Next step if we want stronger regression coverage: add a mocked script-level test for the 404 fallback path and timeout messaging. | scripts/salesforce/data-cloud-upload-csv.ps1; scripts/salesforce/data-cloud-upload-manifest.ps1; scripts/salesforce/DataCloud.Common.ps1; scripts/salesforce/data-cloud-list-jobs.ps1; scripts/salesforce/data-cloud-get-job.ps1; scripts/salesforce/data-cloud-abort-job.ps1; playbooks/data-cloud-upload-csv.md; playbooks/upload-manifest-dataset-to-data-cloud.md; skills/upload-csv-to-data-cloud.md |
| 2026-04-20T02:06:02Z | GitHub Copilot | Phase 6 audit | completed | Verified the Phase 6 README note and Tableau Next playbook are repo-native, tied to current local entry points, and not just a restatement of the upstream evaluation. Original outcome: partially correct. The core claim was accurate, including the missing Tableau Next registry, discovery and inspection wrappers, setup and teardown automation, and local troubleshooting coverage, but the playbook implied a fresh manifest upload as a normal prep step. Fixed that so recent successful jobs are the normal stability check and a re-upload is now explicitly optional. The playbook is actionable for the current repo as a planning and preparation workflow only, not as Tableau Next automation. Next safe implementation step: add read-only PowerShell discovery wrappers for semantic models and workspaces before designing any Tableau Next registry shape. | [playbooks/prepare-tableau-next-semantic-model.md](playbooks/prepare-tableau-next-semantic-model.md); [README.md](README.md); [notes/evaluations/tableau-next-claude-skills.md](notes/evaluations/tableau-next-claude-skills.md); [notes/evaluations/README.md](notes/evaluations/README.md); [skills/INDEX.md](skills/INDEX.md); [CommandCenter TODO.md](CommandCenter TODO.md) |
| 2026-04-20T02:25:06Z | GitHub Copilot | Consolidated handoff | completed | Consolidated handoff for my recent repo work: normalized manifest target registration, unified CSV/schema inference, hardened manifest stream provisioning and upload recovery, refreshed README/playbooks/skills, fixed stale `objectEndpoint` operator messaging, and tightened the Tableau Next prep docs. Overall outcome: partially correct but directionally sound. Local code and docs now match the intended operating model, and current evidence includes PowerShell parse checks, VS Code error scans, helper-command validation, registry refreshes, successful auth resolution, successful CommandCenterAuth deploy after metadata-path correction, and live-job inspection. What still needs fixing is now mostly live-org or reconciliation work: the stream provisioner still does not match at least one existing ACTIVE Data Cloud stream before attempting create, `sunrun-projects` still lacks a saved connector-specific `objectEndpoint`, and current ingest retries are blocked by live stream or job state such as `EDTO ... already in use` and `409 CONFLICT` on an in-progress job. Next step: fix existing-stream resolution in `data-cloud-create-manifest-streams.ps1`, then capture and persist live `objectEndpoint` values so uploads stop depending on generic lookup for the Sunrun targets. | scripts/salesforce/setup-command-center-connected-app.ps1; scripts/salesforce/DataCloud.Common.ps1; scripts/salesforce/data-cloud-register-manifest-targets.ps1; scripts/salesforce/data-cloud-generate-schema.ps1; scripts/salesforce/data-cloud-generate-ingest-metadata.ps1; scripts/salesforce/data-cloud-create-manifest-streams.ps1; scripts/salesforce/data-cloud-upload-csv.ps1; scripts/salesforce/data-cloud-upload-manifest.ps1; scripts/salesforce/data-cloud-list-jobs.ps1; scripts/salesforce/data-cloud-get-job.ps1; scripts/salesforce/data-cloud-abort-job.ps1; README.md; playbooks; skills |
| 2026-04-20T02:25:08Z | GitHub Copilot | Phase 6 handoff | completed | Consolidated handoff for my Tableau Next scope. Changed: added the repo-native starter playbook and README status note, then tightened the playbook so semantic-model prep does not imply a fresh manifest upload or nonexistent local discovery wrappers. Outcome: partially correct after that doc fix; the planning boundary is now accurate. Evidence checked: current README Tableau Next status, playbook prerequisites and exact steps, evaluation notes, existing skill index, and VS Code error scan on the touched docs. Still needs fixing: Tableau Next registry shape, read-only semantic-model and workspace discovery wrappers, setup and teardown automation, and local troubleshooting notes. Next step: implement read-only PowerShell discovery wrappers first, then use their output to decide the registry model. | [playbooks/prepare-tableau-next-semantic-model.md](playbooks/prepare-tableau-next-semantic-model.md); [README.md](README.md); [notes/evaluations/tableau-next-claude-skills.md](notes/evaluations/tableau-next-claude-skills.md); [notes/evaluations/README.md](notes/evaluations/README.md); [skills/INDEX.md](skills/INDEX.md); [CommandCenter TODO.md](CommandCenter TODO.md) |
| 2026-04-20T02:24:55Z | GitHub Copilot | Consolidated handoff | completed | Reviewed my prior Data Cloud changes and validation notes. What changed: Phase 2 now derives stable manifest-backed target rows and preflights stale registry drift; the CommandCenterAuth deployment script already contains the corrected metadata filenames; the live validation run proved auth resolution, manifest registration, and job inspection are reusable repo workflows rather than Sunrun-only wiring. Outcome: mostly correct, but some earlier board notes are stale because the local CommandCenterAuth path defect has already been fixed. Still needs fixing: live stream reconciliation in `data-cloud-create-manifest-streams.ps1` did not match the existing ACTIVE `sunrun2_projects` stream before create, and live ingest state still caused `409 CONFLICT` job creation until the existing job cleared or is aborted. Evidence checked: current script paths match on-disk metadata filenames, prior successful deploy evidence is recorded in the board, `data-cloud-get-access-token.ps1` resolved `salesforce-token-exchange`, manifest registration refreshed the `sunrun-*` rows, and `data-cloud-get-job.ps1` returned the terminal state for the last successful upload job. Next step: harden existing-stream resolution in the manifest stream bootstrap against live stream naming variance, then rerun single-table bootstrap plus upload once the conflicting ingest job is no longer active. | [scripts/salesforce/setup-command-center-connected-app.ps1](scripts/salesforce/setup-command-center-connected-app.ps1); [salesforce/force-app/main/default/extlClntAppGlobalOauthSets/CommandCenterAuth_glbloauth.ecaGlblOauth-meta.xml](salesforce/force-app/main/default/extlClntAppGlobalOauthSets/CommandCenterAuth_glbloauth.ecaGlblOauth-meta.xml); [scripts/salesforce/data-cloud-register-manifest-targets.ps1](scripts/salesforce/data-cloud-register-manifest-targets.ps1); [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1); [scripts/salesforce/data-cloud-get-access-token.ps1](scripts/salesforce/data-cloud-get-access-token.ps1); [scripts/salesforce/data-cloud-get-job.ps1](scripts/salesforce/data-cloud-get-job.ps1); [notes/registries/data-cloud-targets.json](notes/registries/data-cloud-targets.json) |
| 2026-04-20T10:40:00Z | GitHub Copilot | Consolidated handoff | completed | Reviewed my prior validation and corrected the only obvious local concern before handing off: the current workspace no longer has a local CommandCenterAuth path defect, and `setup-command-center-connected-app.ps1 -TargetOrg STORM_TABLEAU_NEXT` now deploys successfully again as job `0AfKZ0000068DyN0AU`. Outcome: the earlier repo-local deploy failure note is stale; the reusable workflow result is otherwise still correct. Evidence checked: current deploy rerun succeeded, Data Cloud auth had already resolved through `salesforce-token-exchange` for `sunrun-projects`, manifest target registration had refreshed the `sunrun-*` rows, and the remaining failures were live-org states in stream creation and ingest job creation rather than manifest-specific hardcoding. Still needs fixing: `data-cloud-create-manifest-streams.ps1` should match pre-existing ACTIVE streams like the current `sunrun2_projects` object before attempting create, and upload retries remain blocked while the live ingest job state is conflicting. Next step: tighten existing-stream resolution in the manifest bootstrap and rerun the single-table `projects` path after the active ingest conflict is cleared or aborted. | [CommandCenter TODO.md](CommandCenter%20TODO.md); [scripts/salesforce/setup-command-center-connected-app.ps1](scripts/salesforce/setup-command-center-connected-app.ps1); [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1); [scripts/salesforce/data-cloud-upload-csv.ps1](scripts/salesforce/data-cloud-upload-csv.ps1); [scripts/salesforce/data-cloud-get-access-token.ps1](scripts/salesforce/data-cloud-get-access-token.ps1); [scripts/salesforce/data-cloud-register-manifest-targets.ps1](scripts/salesforce/data-cloud-register-manifest-targets.ps1) |
| 2026-04-20T10:52:00Z | GitHub Copilot | Stream reconciliation | completed | Fixed `data-cloud-create-manifest-streams.ps1` so existing-stream resolution now matches accepted live object identity from `dataLakeObjectInfo` before create, instead of relying only on connector events, exact stream name, or exact label. Narrow validation against the Sunrun `projects` table succeeded: the bootstrap reused ACTIVE stream `sunrun2_projects_sunrun2_projects_A91ABE55`, kept `resolvedObjectName = sunrun2_projects`, reported `action = reused`, and completed with no registry changes. Outcome for this slice is correct. What still blocks end-to-end success is now outside the stream-bootstrap path: the previously observed ingest-job conflict on upload still has to clear or be aborted before a fresh upload rerun can succeed. | [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1); [CommandCenter TODO.md](CommandCenter%20TODO.md); [tmp/validation-stream-projects/provisioning-state.json](tmp/validation-stream-projects/provisioning-state.json) |
| 2026-04-20T09:57:32Z | GitHub Copilot | Tableau Next discovery | completed | Added the first repo-native read-only Tableau Next discovery wrappers and validated them live against `STORM_TABLEAU_NEXT`. Stable surfaces: `AnalyticsWorkspace` is the workspace inventory source, and `AnalyticsWorkspaceAsset` filtered to `AssetType = 'SemanticModel'` is the stable semantic-model inventory source. Stable output shape: workspace rows now emit `WorkspaceId`, `DeveloperName`, `Label`, `Language`, `Description`, `CreatedDate`, and `LastModifiedDate`; semantic-model rows now emit `WorkspaceId`, workspace name fields, `SemanticModelId`, `WorkspaceAssetId`, `AssetType`, `AssetUsageType`, `HistoricalPromotionStatus`, `CreatedDate`, and `LastModifiedDate`. Remaining gaps: no direct semantic-model detail wrapper yet, no proven semantic-model object with richer fields than the asset inventory row, no Tableau Next registry, and no visualization or dashboard discovery wrapper. Validation: both new scripts executed successfully as live read-only queries. | [scripts/tableau/_TableauNext.Common.ps1](scripts/tableau/_TableauNext.Common.ps1); [scripts/tableau/list-next-workspaces.ps1](scripts/tableau/list-next-workspaces.ps1); [scripts/tableau/list-next-semantic-models.ps1](scripts/tableau/list-next-semantic-models.ps1); [README.md](README.md); [playbooks/prepare-tableau-next-semantic-model.md](playbooks/prepare-tableau-next-semantic-model.md); [.vscode/tasks.json](.vscode/tasks.json) |
| 2026-04-20T02:25:28Z | GitHub Copilot | Consolidated handoff | completed | Reviewed my own scope and found no new local code defect beyond the already-fixed CommandCenterAuth metadata-path literals in `setup-command-center-connected-app.ps1`. Outcome: mostly correct. Reusable parts are working: auth resolution, manifest target refresh, deploy-script preflight, and job inspection. Still needs fixing: `data-cloud-create-manifest-streams.ps1` still fails to reuse the existing live `sunrun2_projects` stream before attempting create, so stream reconciliation remains the main local gap. Evidence checked: the deploy script still points at the real metadata filenames on disk, earlier deploy and registration success is recorded on this board, and `data-cloud-get-job.ps1 -TargetKey sunrun-projects -JobId 1c251a01-54c0-41e4-9a5e-d9a0e2e26b01` now returns `JobComplete`, so the prior upload conflict blocker is stale and cleared. Next step: harden existing-stream matching in `data-cloud-create-manifest-streams.ps1`, then rerun the single-table `projects` bootstrap and upload path to confirm the workflow clears end to end with the live stream state. | [scripts/salesforce/setup-command-center-connected-app.ps1](scripts/salesforce/setup-command-center-connected-app.ps1); [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1); [scripts/salesforce/data-cloud-get-job.ps1](scripts/salesforce/data-cloud-get-job.ps1); [notes/registries/data-cloud-targets.json](notes/registries/data-cloud-targets.json) |
| 2026-04-20T02:26:44Z | GitHub Copilot | Consolidated handoff | completed | Reviewed my recent Data Cloud and docs scope before handoff. What changed: manifest-backed target registration is deterministic, CSV and schema inference are shared, stream bootstrap and upload recovery are hardened, CommandCenterAuth deploy path is corrected, and the related README, playbooks, and skills were tightened to the real automation boundary. Outcome: mostly correct. The repo-local path defect is already fixed and the prior upload-conflict note is now stale because job `1c251a01-54c0-41e4-9a5e-d9a0e2e26b01` currently returns `JobComplete`; remaining failures are still live-stream reconciliation and target metadata gaps, not a new local regression. Evidence checked: successful CommandCenterAuth deploy on this board, successful auth resolution for `sunrun-projects`, manifest target refresh, and a fresh `data-cloud-get-job.ps1` recheck showing `state = JobComplete` for the previously conflicting ingest job. Still needs fixing: `data-cloud-create-manifest-streams.ps1` does not yet reuse the existing ACTIVE `sunrun2_projects` stream before create, and `sunrun-projects` still depends on generic lookup until a connector-specific `objectEndpoint` is captured. Next step: fix existing-stream matching in the manifest bootstrap, then persist the live `objectEndpoint` for `sunrun-projects` and rerun the single-table `projects` flow. | [scripts/salesforce/setup-command-center-connected-app.ps1](scripts/salesforce/setup-command-center-connected-app.ps1); [scripts/salesforce/DataCloud.Common.ps1](scripts/salesforce/DataCloud.Common.ps1); [scripts/salesforce/data-cloud-register-manifest-targets.ps1](scripts/salesforce/data-cloud-register-manifest-targets.ps1); [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1); [scripts/salesforce/data-cloud-upload-csv.ps1](scripts/salesforce/data-cloud-upload-csv.ps1); [scripts/salesforce/data-cloud-get-job.ps1](scripts/salesforce/data-cloud-get-job.ps1); [README.md](README.md); [playbooks/guided-data-cloud-manifest-setup.md](playbooks/guided-data-cloud-manifest-setup.md); [playbooks/upload-manifest-dataset-to-data-cloud.md](playbooks/upload-manifest-dataset-to-data-cloud.md); [skills/guided-data-cloud-manifest-setup.md](skills/guided-data-cloud-manifest-setup.md); [skills/upload-csv-to-data-cloud.md](skills/upload-csv-to-data-cloud.md) |
| 2026-04-20T11:02:00Z | GitHub Copilot | Target metadata cleanup | in progress | Claimed live connector-specific objectEndpoint persistence for Sunrun targets. Narrow scope: persist the real live endpoint values from manifest bootstrap, keep dataset-derived fields deterministic, and verify upload stops treating generic lookup as the normal path when endpoints are present. | [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1); [scripts/salesforce/DataCloud.Common.ps1](scripts/salesforce/DataCloud.Common.ps1); [scripts/salesforce/data-cloud-upload-csv.ps1](scripts/salesforce/data-cloud-upload-csv.ps1); [notes/registries/data-cloud-targets.json](notes/registries/data-cloud-targets.json) |
| 2026-04-20T03:07:16Z | GitHub Copilot | Target metadata cleanup | completed | Persisted live `objectEndpoint` values for the active `sunrun-*` manifest targets by fixing registry sync in `data-cloud-create-manifest-streams.ps1` to match registry rows by manifest table identity instead of only `dataStreamLabel`. After rerunning manifest bootstrap, these targets now keep connector-specific endpoint values in the registry: `sunrun-projects`, `sunrun-affiliates`, `sunrun-distributors`, `sunrun-milestones`, `sunrun-bom_lines`, and `sunrun-procurement_orders`, each with `objectEndpoint` equal to the live accepted `sunrun2_*` object name. Upload consumption was also hardened: `data-cloud-upload-csv.ps1` now treats configured-endpoint `400/404` responses as a non-fatal fallback to generic lookup, and the warning message no longer assumes every endpoint failure means stale metadata. Validation: PowerShell parse plus VS Code error scan on the touched scripts; one-table bootstrap showed `registrySync.updatedCount=1` for `sunrun-projects`; full Sunrun bootstrap showed `registrySync.updatedCount=5`; registry inspection confirmed all six active `sunrun-*` rows now have `objectEndpoint`; and `data-cloud-upload-csv.ps1 -TargetKey sunrun-affiliates -CsvPath '.\\Demos\\Sunrun Demo\\affiliates.csv'` reached `JobComplete` as job `22d89031-a2a8-4d67-94a6-55f9070bdb8f`. Current normal-operation result: uploads no longer depend on generic lookup because of missing metadata, but this org still falls back to generic lookup after the configured endpoint route returns `400 BAD_REQUEST`, so generic fallback remains necessary until the connector-specific route contract is clarified. | [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1); [scripts/salesforce/data-cloud-upload-csv.ps1](scripts/salesforce/data-cloud-upload-csv.ps1); [notes/registries/data-cloud-targets.json](notes/registries/data-cloud-targets.json); [tmp/validation-stream-projects-endpoint/provisioning-state.json](tmp/validation-stream-projects-endpoint/provisioning-state.json); [tmp/validation-stream-all-endpoints/provisioning-state.json](tmp/validation-stream-all-endpoints/provisioning-state.json) |
| 2026-04-20T03:14:31Z | GitHub Copilot | Tableau Next target registry | completed | Added the minimum non-secret Tableau Next SDM target registry and inspect flow. Required fields are now `key`, `targetOrg`, `workspaceId`, and `workspaceDeveloperName`; `workspaceLabel` is persisted as a human-check field, while `semanticModelId`, `workspaceAssetId`, and `assetUsageType` stay optional and are only saved when live semantic-model discovery proves them. New scripts: `register-next-target.ps1` validates live workspace discovery before writing the registry, and `inspect-next-target.ps1` revalidates the saved target and reports `ReadyForCreation` for workspace-only targets or validates a pinned existing model when one is saved. Validation: PowerShell parse plus VS Code error scan on the new and refactored Tableau Next scripts; live discovery samples from `list-next-workspaces.ps1` and `list-next-semantic-models.ps1`; live target registration for `juju-demo-sdm`; and live inspection returning `InspectionStatus = ReadyForCreation` for workspace `1DyKZ000000kA8f0AE` in `STORM_TABLEAU_NEXT`. What still cannot be inferred automatically: richer semantic-model detail than the asset inventory row, a stable semantic-model name to track, the correct SDM creation payload, and visualization or dashboard relationships. | [scripts/tableau/_TableauNext.Common.ps1](scripts/tableau/_TableauNext.Common.ps1); [scripts/tableau/list-next-workspaces.ps1](scripts/tableau/list-next-workspaces.ps1); [scripts/tableau/list-next-semantic-models.ps1](scripts/tableau/list-next-semantic-models.ps1); [scripts/tableau/register-next-target.ps1](scripts/tableau/register-next-target.ps1); [scripts/tableau/inspect-next-target.ps1](scripts/tableau/inspect-next-target.ps1); [notes/registries/tableau-next-targets.json](notes/registries/tableau-next-targets.json); [playbooks/prepare-tableau-next-semantic-model.md](playbooks/prepare-tableau-next-semantic-model.md); [skills/inspect-tableau-next-target.md](skills/inspect-tableau-next-target.md); [README.md](README.md) |
| 2026-04-20T08:02:42Z | GitHub Copilot | Tableau Next discoverability | in progress | Claimed the follow-up pass for Tableau Next operator discoverability. Scope is VS Code tasks, README/playbook consistency, and skill coverage for the new workspace target and SDM helper scripts only. | [scripts/tableau/upsert-next-semantic-model.ps1](scripts/tableau/upsert-next-semantic-model.ps1); [.vscode/tasks.json](.vscode/tasks.json); [README.md](README.md); [skills/INDEX.md](skills/INDEX.md) |
| 2026-04-20T08:04:30Z | GitHub Copilot | Tableau Next discoverability | completed | Added operator discoverability for the new Tableau Next flows. VS Code tasks now cover `register-next-target.ps1`, `inspect-next-target.ps1`, and dry-run request generation through `upsert-next-semantic-model.ps1`; the README no longer claims the target registry or setup helper are missing; and the skill index now includes a focused skill for building the SDM request from a saved target. Outcome for this slice is correct. Validation: VS Code error scan on `.vscode/tasks.json`, `README.md`, `skills/INDEX.md`, and the new `skills/build-tableau-next-semantic-model-request.md`. Remaining gap: direct semantic-model detail inspection is still missing, so the operator still has to treat the current helper as request generation first and live write second. | [.vscode/tasks.json](.vscode/tasks.json); [README.md](README.md); [skills/INDEX.md](skills/INDEX.md); [skills/build-tableau-next-semantic-model-request.md](skills/build-tableau-next-semantic-model-request.md) |
| 2026-04-20T11:20:00Z | GitHub Copilot | Tableau Next SDM helper | in progress | Claimed the first repo-native Tableau Next semantic-model create or update helper. Narrow scope: dry-run-first spec and Aura envelope generation from a saved Tableau Next target plus known Data Cloud object names, with optional live apply only when a local Aura session context is provided explicitly. | [scripts/tableau/_TableauNext.Common.ps1](scripts/tableau/_TableauNext.Common.ps1); [scripts/tableau/upsert-next-semantic-model.ps1](scripts/tableau/upsert-next-semantic-model.ps1); [playbooks/prepare-tableau-next-semantic-model.md](playbooks/prepare-tableau-next-semantic-model.md); [README.md](README.md) |
| 2026-04-20T11:34:00Z | GitHub Copilot | Tableau Next SDM helper | completed | Added the first repo-native Tableau Next semantic-model create or update helper as a dry-run-first wrapper. `upsert-next-semantic-model.ps1` now reuses the saved Tableau Next target, normalizes model and object inputs, emits a repo-owned semantic-model spec plus an inferred Aura request for `CreateSemanticDataModelController/ACTION$autoSdm`, and can optionally attempt live apply only when explicit local Aura context, token, and cookies are provided. Shared support in `_TableauNext.Common.ps1` now includes API-name normalization plus explicit Aura-session and action helpers. Validation: PowerShell parse on `_TableauNext.Common.ps1` and `upsert-next-semantic-model.ps1`; dry-run execution against live target `juju-demo-sdm`; and a round-trip check where the helper wrote `tmp/juju-demo-sdm.request.json` and then reused that same file through `-SpecPath`. Remaining gap: the live semantic-model write contract is still inferred rather than proven, so the helper should be treated as the repo-native request generator first and a live write wrapper second until a successful create or update response is captured and folded back into the local contract. | [scripts/tableau/_TableauNext.Common.ps1](scripts/tableau/_TableauNext.Common.ps1); [scripts/tableau/upsert-next-semantic-model.ps1](scripts/tableau/upsert-next-semantic-model.ps1); [README.md](README.md); [playbooks/prepare-tableau-next-semantic-model.md](playbooks/prepare-tableau-next-semantic-model.md); [tmp/juju-demo-sdm.request.json](tmp/juju-demo-sdm.request.json) |
| 2026-04-20T08:14:34Z | GitHub Copilot | Tableau Next SDM validation | in progress | Claimed the final validation pass for saved target `juju-demo-sdm`. Scope is inspect target, reconfirm the validated Data Cloud object names, reuse or regenerate the SDM spec, attempt live apply only if explicit Aura session inputs exist locally, then re-inspect target and semantic-model inventory and post an evidence-backed result. | [scripts/tableau/inspect-next-target.ps1](scripts/tableau/inspect-next-target.ps1); [scripts/tableau/list-next-semantic-models.ps1](scripts/tableau/list-next-semantic-models.ps1); [scripts/tableau/upsert-next-semantic-model.ps1](scripts/tableau/upsert-next-semantic-model.ps1); [notes/registries/tableau-next-targets.json](notes/registries/tableau-next-targets.json); [tmp/juju-demo-sdm.request.json](tmp/juju-demo-sdm.request.json); [CommandCenter TODO.md](CommandCenter TODO.md) |
| 2026-04-20T08:17:57Z | GitHub Copilot | Tableau Next SDM validation | completed | Final validation for saved target `juju-demo-sdm` is blocked before live write. Exact commands run: `powershell -ExecutionPolicy Bypass -File .\scripts\tableau\inspect-next-target.ps1 -TargetKey juju-demo-sdm -Json`; `powershell -ExecutionPolicy Bypass -File .\scripts\tableau\list-next-semantic-models.ps1 -TargetOrg STORM_TABLEAU_NEXT -WorkspaceId 1DyKZ000000kA8f0AE -Json`; `[pscustomobject]@{ AuraContext = (Test-Path '.\tmp\aura-context.json'); AuraToken = (Test-Path '.\tmp\aura-token.txt'); AuraCookies = (Test-Path '.\tmp\aura-cookies.txt'); CookieHeader = (Test-Path '.\tmp\aura-cookie-header.txt') } | ConvertTo-Json -Compress`; `[pscustomobject]@{ TableauNextAuraToken = (-not [string]::IsNullOrWhiteSpace($env:TABLEAU_NEXT_AURA_TOKEN)); TableauNextAuraCookie = (-not [string]::IsNullOrWhiteSpace($env:TABLEAU_NEXT_AURA_COOKIE_HEADER)); TableauNextAuraContext = (-not [string]::IsNullOrWhiteSpace($env:TABLEAU_NEXT_AURA_CONTEXT)) } | ConvertTo-Json -Compress`; `$rows = Get-Content '.\notes\registries\data-cloud-targets.json' -Raw | ConvertFrom-Json; $rows.targets | Where-Object { $_.key -in @('sunrun-projects','sunrun-affiliates') } | Select-Object key, objectName, objectEndpoint, salesforceAlias, sourceName | ConvertTo-Json -Compress`; and `powershell -ExecutionPolicy Bypass -File .\scripts\tableau\upsert-next-semantic-model.ps1 -TargetKey juju-demo-sdm -SpecPath '.\tmp\juju-demo-sdm.request.json' -Json`. Evidence: `inspect-next-target.ps1` returned `InspectionStatus = ReadyForCreation`; the current workspace inventory still shows the same five existing semantic models before and after the dry-run validation; the saved spec still round-trips as `RequestedAction = Create` for workspace `1DyKZ000000kA8f0AE`; and the Data Cloud object names referenced by the spec are still the validated live names `sunrun2_projects` and `sunrun2_affiliates` from the registry. Live apply was not attempted because supported explicit Aura inputs are incomplete locally: `tmp/aura-context.json` and `tmp/aura-cookies.txt` exist, but no `tmp/aura-token.txt` exists and no `TABLEAU_NEXT_AURA_TOKEN` env var is set. Status: blocked. Primary blocker classification: auth/permissions, because the helper requires an explicit Aura token for live apply. Secondary caveat: the live write contract is still inferred rather than proven, so even with a token the first create should still be treated as contract validation. Single next best step: capture a current Aura token for the active `STORM_TABLEAU_NEXT` Tableau Next browser session and rerun `upsert-next-semantic-model.ps1 -Apply` with the existing spec plus explicit context, token, and cookies to prove or falsify the live write contract. | [scripts/tableau/inspect-next-target.ps1](scripts/tableau/inspect-next-target.ps1); [scripts/tableau/list-next-semantic-models.ps1](scripts/tableau/list-next-semantic-models.ps1); [scripts/tableau/upsert-next-semantic-model.ps1](scripts/tableau/upsert-next-semantic-model.ps1); [notes/registries/tableau-next-targets.json](notes/registries/tableau-next-targets.json); [notes/registries/data-cloud-targets.json](notes/registries/data-cloud-targets.json); [tmp/juju-demo-sdm.request.json](tmp/juju-demo-sdm.request.json); [CommandCenter TODO.md](CommandCenter TODO.md) |
| 2026-04-20T10:20:00Z | GitHub Copilot | Tableau Next SDM proof | in progress | Claimed the final proof-or-blocker pass for `juju-demo-sdm`. Narrow scope: add the minimum semantic-model detail inspection on the existing `AnalyticsWorkspaceAsset` surface, reuse recovered local Aura session artifacts if valid, attempt one live `upsert-next-semantic-model.ps1 -Apply`, then re-inspect inventory and detail and record either hard publish evidence or one sharply classified blocker. | [scripts/tableau/_TableauNext.Common.ps1](scripts/tableau/_TableauNext.Common.ps1); [scripts/tableau/inspect-next-target.ps1](scripts/tableau/inspect-next-target.ps1); [scripts/tableau/list-next-semantic-models.ps1](scripts/tableau/list-next-semantic-models.ps1); [scripts/tableau/upsert-next-semantic-model.ps1](scripts/tableau/upsert-next-semantic-model.ps1); [CommandCenter TODO.md](CommandCenter TODO.md) |
| 2026-04-20T10:38:00Z | GitHub Copilot | Tableau Next SDM proof | completed | Final result is a single sharply defined blocker, not proven publish. Repo-local fixes completed first: added `inspect-next-semantic-model.ps1` as the missing minimal detail wrapper on `AnalyticsWorkspaceAsset`, extended `_TableauNext.Common.ps1` with targeted semantic-model detail lookup and Aura HTTP error-body capture, and fixed a live-apply helper defect in `upsert-next-semantic-model.ps1` where `ShouldProcess` incorrectly referenced `workspace.WorkspaceLabel` instead of `workspace.Label`. Validation sequence and evidence: `inspect-next-target.ps1 -TargetKey juju-demo-sdm -Json` still returns `ReadyForCreation`; the new detail script validates an existing semantic model in workspace `1DyKZ000000kA8f0AE`; two live `upsert-next-semantic-model.ps1 -Apply` attempts were made using recovered local Aura tokens plus both `tmp/aura-cookies.txt` and `tmp/aura-live-cookies.txt`; both requests reached the server and both failed with the same Aura payload `{"event":{"descriptor":"markup://aura:invalidSession"...}}`; and the post-attempt semantic-model inventory for workspace `1DyKZ000000kA8f0AE` still shows the same five models, while `inspect-next-target.ps1` still reports `ReadyForCreation`, so no new semantic model was created or partially pinned. Final blocker classification: auth/session input. Exact next required action: capture a fresh Aura browser session for the active `STORM_TABLEAU_NEXT` Tableau Next page, then rerun `upsert-next-semantic-model.ps1 -Apply` with that fresh `-AuraContextPath` or `-AuraContextJson`, `-AuraToken` or `-AuraTokenPath`, and `-CookiePath`; do not rely on the currently saved `tmp/` session artifacts because they now produce `aura:invalidSession` on live apply. | [scripts/tableau/_TableauNext.Common.ps1](scripts/tableau/_TableauNext.Common.ps1); [scripts/tableau/inspect-next-semantic-model.ps1](scripts/tableau/inspect-next-semantic-model.ps1); [scripts/tableau/upsert-next-semantic-model.ps1](scripts/tableau/upsert-next-semantic-model.ps1); [scripts/tableau/inspect-next-target.ps1](scripts/tableau/inspect-next-target.ps1); [scripts/tableau/list-next-semantic-models.ps1](scripts/tableau/list-next-semantic-models.ps1); [tmp/juju-demo-sdm.request.json](tmp/juju-demo-sdm.request.json); [CommandCenter TODO.md](CommandCenter TODO.md) |
| 2026-04-20T03:16:47Z | GitHub Copilot | Projects validation | completed | Narrow projects-table validation only, after the upstream stream-reuse and target-metadata fixes were complete. Commands run exactly: `powershell -ExecutionPolicy Bypass -File .\scripts\salesforce\data-cloud-get-access-token.ps1 -TargetKey sunrun-projects -AsJson`; `powershell -ExecutionPolicy Bypass -File .\scripts\salesforce\data-cloud-register-manifest-targets.ps1 -ManifestPath '.\Demos\Sunrun Demo\manifest.json' -TargetKeyPrefix sunrun`; `powershell -ExecutionPolicy Bypass -File .\scripts\salesforce\data-cloud-create-manifest-streams.ps1 -ManifestPath '.\Demos\Sunrun Demo\manifest.json' -TargetOrg STORM_TABLEAU_NEXT_DC -SourceName sunrun_ingest_api -ObjectNamePrefix sunrun2 -Tables projects -OutputRoot '.\tmp\validation-stream-projects' -Force`; `powershell -ExecutionPolicy Bypass -File .\scripts\salesforce\data-cloud-upload-csv.ps1 -TargetKey sunrun-projects -CsvPath '.\tmp\phase8-projects-sample.csv'`; and `powershell -ExecutionPolicy Bypass -File .\scripts\salesforce\data-cloud-get-job.ps1 -TargetKey sunrun-projects -JobId ef88f04b-b348-48fd-94aa-f439fff75f07`. Success points: auth resolved through `salesforce-token-exchange` for alias `STORM_TABLEAU_NEXT_DC`; manifest target refresh preserved `objectEndpoint = sunrun2_projects`; stream bootstrap reused existing ACTIVE stream `sunrun2_projects_sunrun2_projects_A91ABE55` with `action = reused`; upload created job `ef88f04b-b348-48fd-94aa-f439fff75f07`; and explicit job inspection returned `state = JobComplete` for object `sunrun2_projects`. Remaining failure or caveat: the configured object-endpoint route still returns `400 BAD_REQUEST` on create for `sunrun-projects`, so the upload succeeds only after the hardened fallback to generic object lookup. Reusability verdict: the projects flow is now reusable end to end for this repo and org, but not yet cleanly on the connector-specific endpoint path; generic fallback is still part of the working path until that route contract is clarified. | [scripts/salesforce/data-cloud-get-access-token.ps1](scripts/salesforce/data-cloud-get-access-token.ps1); [scripts/salesforce/data-cloud-register-manifest-targets.ps1](scripts/salesforce/data-cloud-register-manifest-targets.ps1); [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1); [scripts/salesforce/data-cloud-upload-csv.ps1](scripts/salesforce/data-cloud-upload-csv.ps1); [scripts/salesforce/data-cloud-get-job.ps1](scripts/salesforce/data-cloud-get-job.ps1); [notes/registries/data-cloud-targets.json](notes/registries/data-cloud-targets.json); [tmp/validation-stream-projects/provisioning-state.json](tmp/validation-stream-projects/provisioning-state.json) |
| 2026-04-20T21:00:00Z | GitHub Copilot | Zero-touch orchestration | completed | Implemented the repo-local authenticated-to-SDM orchestration path. Automated now: `scripts/salesforce/orchestrate-authenticated-to-sdm.ps1` verifies standard Salesforce auth, deploys or re-verifies `CommandCenterAuth`, refreshes manifest-backed Data Cloud targets, validates Data Cloud token exchange, preflights connector existence, reconciles streams, derives stable Data Cloud object names for SDM, resolves or registers a Tableau Next target from live workspace discovery, directly inspects semantic-model inventory, generates the SDM request, and emits a machine-readable readiness state. Added discovery-driven Tableau Next target registration by `WorkspaceDeveloperName` or label, plus direct semantic-model inspection and new docs/skills/gap report for the zero-touch path and its boundary. Validation: live `register-next-target.ps1` by `WorkspaceDeveloperName = Juju_Demo`; live `inspect-next-semantic-model.ps1` for semantic model `2SMKZ000000kAaL4AU`; and a dry-run orchestration run against the Sunrun manifest for tables `projects,affiliates`, which produced `tmp/sunrun-demo-authenticated-to-sdm-state.json` with classification `ReadyThroughDryRunOnly` after successful auth, connector preflight, ACTIVE stream reuse, Tableau Next target registration, and semantic-model request generation. What still requires human action: only stable workspace routing criteria when multiple workspaces are visible and no saved target exists, plus explicit Aura session inputs if a live SDM apply is desired. What still requires platform capability the repo cannot control: Data Cloud connector creation in the org, a stable public Tableau Next semantic-model write API beyond the inferred Aura action, and connector-specific ingest route behavior that still requires generic upload fallback in some orgs. Next best strategic investment: prove and wrap the live Tableau Next semantic-model write contract so the readiness classification can move from `ReadyThroughDryRunOnly` to `ReadyForFullAutomation`. | [scripts/salesforce/orchestrate-authenticated-to-sdm.ps1](scripts/salesforce/orchestrate-authenticated-to-sdm.ps1); [scripts/tableau/_TableauNext.Common.ps1](scripts/tableau/_TableauNext.Common.ps1); [scripts/tableau/register-next-target.ps1](scripts/tableau/register-next-target.ps1); [scripts/tableau/inspect-next-semantic-model.ps1](scripts/tableau/inspect-next-semantic-model.ps1); [README.md](README.md); [playbooks/run-authenticated-to-sdm.md](playbooks/run-authenticated-to-sdm.md); [skills/INDEX.md](skills/INDEX.md); [skills/authenticated-to-sdm-orchestration.md](skills/authenticated-to-sdm-orchestration.md); [skills/inspect-next-semantic-model.md](skills/inspect-next-semantic-model.md); [notes/evaluations/authenticated-to-sdm-gap-report-2026-04-20.md](notes/evaluations/authenticated-to-sdm-gap-report-2026-04-20.md); [tmp/sunrun-demo-authenticated-to-sdm-state.json](tmp/sunrun-demo-authenticated-to-sdm-state.json) |
| 2026-04-20T23:40:00Z | GitHub Copilot | End-to-end validation | completed | Validated the current authenticated-to-SDM path against live post-handoff evidence. Proven now: `scripts/salesforce/orchestrate-authenticated-to-sdm.ps1 -ApplySemanticModel` can complete manifest target refresh, Data Cloud token exchange, connector preflight, ACTIVE stream reuse, Tableau Next target registration, semantic-model create, relationship publish, and model validation in one orchestrated run. Hard evidence: `tmp/sunrun-authenticated-to-sdm-apply-state.json` reports `readiness.classification = ReadyForFullAutomation`, `semanticModelRequest.status = Applied`, `AppliedModelId = 2SMKZ000000kAfG4AU`, `AppliedModelApiName = Sunrun_Residential_Solar_Operations_Semantic_Model_20260420_2358`, and `Validation.isValid = true`; the same artifact shows five manifest relationships persisted on the created model. Exact auth boundary that was actually validated: this is zero-touch after a usable Salesforce CLI session already exists and Data Cloud auth can resolve through token exchange; it is not yet proven as a cold-start one-browser-auth experience for a brand-new org because the orchestration still requires the standard Salesforce org alias to exist first and only self-bootstraps the dedicated Data Cloud login when token exchange cannot already resolve. | [scripts/salesforce/orchestrate-authenticated-to-sdm.ps1](scripts/salesforce/orchestrate-authenticated-to-sdm.ps1); [scripts/tableau/upsert-next-semantic-model.ps1](scripts/tableau/upsert-next-semantic-model.ps1); [tmp/sunrun-authenticated-to-sdm-apply-state.json](tmp/sunrun-authenticated-to-sdm-apply-state.json); [tmp/sunrun-demo.semantic-model.spec.json](tmp/sunrun-demo.semantic-model.spec.json); [tmp/sunrun-demo.semantic-model.request.json](tmp/sunrun-demo.semantic-model.request.json) |
| 2026-04-20T23:55:00Z | GitHub Copilot | Tableau Next finalization | in progress | Claimed the final Tableau Next SDM closure pass. Scope: verify the current repo-owned semantic-model helper contract on the saved target path, prove live create or update from saved target plus validated Data Cloud objects if still needed, otherwise reduce the remaining gap to the smallest materially wrong docs or board state and correct only that. | [scripts/tableau/_TableauNext.Common.ps1](scripts/tableau/_TableauNext.Common.ps1); [scripts/tableau/list-next-semantic-models.ps1](scripts/tableau/list-next-semantic-models.ps1); [scripts/tableau/inspect-next-target.ps1](scripts/tableau/inspect-next-target.ps1); [scripts/tableau/upsert-next-semantic-model.ps1](scripts/tableau/upsert-next-semantic-model.ps1); [notes/registries/tableau-next-targets.json](notes/registries/tableau-next-targets.json); [playbooks/prepare-tableau-next-semantic-model.md](playbooks/prepare-tableau-next-semantic-model.md); [README.md](README.md); [tmp/juju-demo-sdm.request.json](tmp/juju-demo-sdm.request.json) |
| 2026-04-21T05:44:48Z | GitHub Copilot | Tableau Next finalization | completed | Closed the remaining saved-target SDM gap with one local helper repair and one saved-target proof run. What changed: `scripts/tableau/_TableauNext.Common.ps1` and `scripts/tableau/upsert-next-semantic-model.ps1` now fall back from live DLO `describe` 404s to repo-generated Data Cloud field metadata under `salesforce/generated/**/objects/*__dll/fields/*.field-meta.xml`, including a stripped table-name fallback so active names like `sunrun2_affiliates` can reuse generated metadata stored under neutral object names such as `affiliates__dll`; the helper output no longer frames Aura as the live contract, and `tmp/juju-demo-sdm.request.json` was regenerated through the repaired path. Exact validation commands run: `powershell -ExecutionPolicy Bypass -File .\scripts\tableau\inspect-next-target.ps1 -TargetKey juju-demo-sdm -Json`; `powershell -ExecutionPolicy Bypass -File .\scripts\tableau\list-next-semantic-models.ps1 -TargetOrg STORM_TABLEAU_NEXT -WorkspaceId 1DyKZ000000kA8f0AE -Json`; `$outputPath = '.\tmp\juju-demo-sdm.request.json'; & .\scripts\tableau\upsert-next-semantic-model.ps1 -TargetKey juju-demo-sdm -ModelApiName Juju_Demo_Semantic -ModelLabel 'Juju Demo Semantic' -ObjectApiName sunrun2_projects,sunrun2_affiliates -PrimaryObjectApiName sunrun2_projects -OutputPath $outputPath -Json`; `powershell -ExecutionPolicy Bypass -File .\scripts\tableau\upsert-next-semantic-model.ps1 -TargetKey juju-demo-sdm -SpecPath .\tmp\juju-demo-sdm.request.json -Json`; `$stamp = Get-Date -Format 'yyyyMMdd_HHmmss'; $apiName = 'Juju_Demo_Sunrun_Semantic_' + $stamp; $label = 'Juju Demo Sunrun Semantic ' + $stamp; & .\scripts\tableau\upsert-next-semantic-model.ps1 -TargetKey juju-demo-sdm -SpecPath .\tmp\sunrun-demo.semantic-model.spec.json -ModelApiName $apiName -ModelLabel $label -Apply -Confirm:$false -Json`; `. .\scripts\tableau\_TableauNext.Common.ps1; $model = Get-TableauNextSemanticModelDefinition -TargetOrg STORM_TABLEAU_NEXT -SemanticModelIdOrApiName '2SMKZ000000kAfa4AE'; [pscustomobject]@{ Id = $model.id; ApiName = $model.apiName; RelationshipCount = @($model.semanticRelationships).Count } | ConvertTo-Json -Compress`; and `. .\scripts\tableau\_TableauNext.Common.ps1; $context = Get-TableauNextAccessContext -TargetOrg STORM_TABLEAU_NEXT; $validation = Invoke-TableauNextApiRequest -Context $context -Method Get -RelativePath 'ssot/semantic/models/2SMKZ000000kAfa4AE/validate'; [pscustomobject]@{ IsValid = $validation.isValid } | ConvertTo-Json -Compress`. Result: SDM publishing succeeded on the saved-target path using the manifest-backed spec through `upsert-next-semantic-model.ps1`. Hard evidence: workspace inventory now includes newly created semantic model `2SMKZ000000kAfa4AE` created at `2026-04-21T05:42:45Z`; direct detail inspection returns `ApiName = Juju_Demo_Sunrun_Semantic_20260420_224235` and `RelationshipCount = 5`; direct validation returns `IsValid = true`. Narrow caveat left after success: ad hoc live create from only `-ObjectApiName sunrun2_projects,sunrun2_affiliates` still returned `400 Bad Request`, so the fully proven live path remains saved target plus manifest-backed spec rather than saved target plus bare object names only. Single next best step: if we want to remove that remaining caveat, capture the exact POST body diff between the successful manifest-backed create and the failing bare-object create, then fold the missing required fields into the object-only generation path. | [scripts/tableau/_TableauNext.Common.ps1](scripts/tableau/_TableauNext.Common.ps1); [scripts/tableau/upsert-next-semantic-model.ps1](scripts/tableau/upsert-next-semantic-model.ps1); [tmp/juju-demo-sdm.request.json](tmp/juju-demo-sdm.request.json); [tmp/sunrun-demo.semantic-model.spec.json](tmp/sunrun-demo.semantic-model.spec.json); [CommandCenter TODO.md](CommandCenter%20TODO.md) |
| 2026-04-21T05:50:00Z | GitHub Copilot | Repo cleanup staging | in progress | Claimed the repo cleanup pass. Scope: create one non-destructive recycling-bin staging area, preserve current agent-used and current product workflow docs, and move stale evaluation, Aura-era, and other no-longer-needed supporting surfaces out of the active repo tree without permanently deleting them. | [README.md](README.md); [skills](skills); [playbooks/prepare-tableau-next-semantic-model.md](playbooks/prepare-tableau-next-semantic-model.md); [scripts/tableau](scripts/tableau); [notes/evaluations](notes/evaluations); [tmp/](tmp/) |
| 2026-04-21T06:12:00Z | GitHub Copilot | Repo cleanup staging | completed | Finished the first aggressive non-destructive cleanup pass. Created `recycling-bin/2026-04-21-repo-cleanup/` as the single staging area, moved the stale `notes/evaluations/` and `prompts/` trees out of the active repo, moved the standalone Aura script `scripts/salesforce/invoke-aura-action.ps1`, and staged 180+ Aura-era and old validation/probe artifacts out of `tmp/` while keeping the current proven SDM request/spec/state artifacts in place. Also removed dead Aura-era code from the active path by deleting unreferenced Aura helper functions from `scripts/tableau/_TableauNext.Common.ps1` and dropping unreferenced Aura parameters from `scripts/salesforce/orchestrate-authenticated-to-sdm.ps1`. Validation: PowerShell errors check returned clean for both edited scripts; repo search no longer finds `Get-TableauNextAuraApplyReadiness`, `Get-TableauNextAuraSession`, `Invoke-TableauNextAuraAction`, or `invoke-aura-action.ps1` under `scripts/`; and the active `tmp/` root now retains only current request/spec/state and a small set of still-unreviewed non-Aura artifacts. | [recycling-bin/2026-04-21-repo-cleanup/README.md](recycling-bin/2026-04-21-repo-cleanup/README.md); [scripts/tableau/_TableauNext.Common.ps1](scripts/tableau/_TableauNext.Common.ps1); [scripts/salesforce/orchestrate-authenticated-to-sdm.ps1](scripts/salesforce/orchestrate-authenticated-to-sdm.ps1); [recycling-bin/2026-04-21-repo-cleanup/tmp](recycling-bin/2026-04-21-repo-cleanup/tmp); [tmp/](tmp/) |
| 2026-04-20T23:24:43Z | GitHub Copilot | Zero-touch consistency | in progress | Claimed the narrow consistency pass after the runtime authenticated-to-SDM and saved-target SDM paths were already proven. Scope: correct stale zero-touch boundary docs and gap-report language, recreate a live evaluation note after cleanup moved the old one to recycling, add the missing end-to-end task surface, and keep the runtime code unchanged unless validation finds a local defect. | [README.md](README.md); [playbooks/prepare-tableau-next-semantic-model.md](playbooks/prepare-tableau-next-semantic-model.md); [playbooks/run-authenticated-to-sdm.md](playbooks/run-authenticated-to-sdm.md); [.vscode/tasks.json](.vscode/tasks.json); [notes/evaluations](notes/evaluations) |
| 2026-04-20T23:36:29Z | GitHub Copilot | Zero-touch consistency | completed | Finished the narrow zero-touch consistency pass. What changed: repaired and validated `.vscode/tasks.json`, added end-to-end tasks for `scripts/salesforce/orchestrate-authenticated-to-sdm.ps1` plus direct semantic-model inspection, updated `README.md` and the authenticated-to-SDM and Tableau Next playbooks to match the proven boundary, and recreated a live gap report under `notes/evaluations/` after cleanup had moved the older report into recycling. Boundary now documented precisely: the repo is proven for near-zero-touch execution after a usable standard Salesforce CLI session exists; Data Cloud connector creation remains platform-bound; and the remaining narrower repo-local caveat is the less-proven object-only SDM create path without a manifest-backed spec. Validation: Node JSON parse succeeded for `.vscode/tasks.json`; the repaired file now ends at the first valid JSON object with no appended garbage; and the updated docs now match the already-proven orchestration and saved-target SDM publish evidence recorded earlier on the board. | [.vscode/tasks.json](.vscode/tasks.json); [README.md](README.md); [playbooks/run-authenticated-to-sdm.md](playbooks/run-authenticated-to-sdm.md); [playbooks/prepare-tableau-next-semantic-model.md](playbooks/prepare-tableau-next-semantic-model.md); [notes/evaluations/authenticated-to-sdm-gap-report-2026-04-20.md](notes/evaluations/authenticated-to-sdm-gap-report-2026-04-20.md); [CommandCenter TODO.md](CommandCenter%20TODO.md) |

Message format to append:

| UTC | Agent | Phase | Status | Message | Files / Blockers |
| --- | --- | --- | --- | --- | --- |
| 2026-04-19T12:00:00Z | Agent-A | Phase 1 | in progress | Claimed the CommandCenterAuth hardening work. | [scripts/salesforce/setup-command-center-connected-app.ps1](scripts/salesforce/setup-command-center-connected-app.ps1) |
| 2026-04-20T00:00:00Z | GitHub Copilot | Phase 1 | completed | Hardened CommandCenterAuth deployment preflight and failure output, made LaunchLogin require a separate Data Cloud alias, updated README and playbook to the real deployment chain, and documented that fresh orgs still need manual Setup for app authorization visibility, Data Cloud connector creation, and data streams. Validation: PowerShell parse plus VS Code error scan on the edited scripts. | [scripts/salesforce/setup-command-center-connected-app.ps1](scripts/salesforce/setup-command-center-connected-app.ps1), [scripts/salesforce/data-cloud-login-web.ps1](scripts/salesforce/data-cloud-login-web.ps1), [playbooks/set-up-command-center-connected-app.md](playbooks/set-up-command-center-connected-app.md), [README.md](README.md), [notes/evaluations/salesforce-auth-strategy.md](notes/evaluations/salesforce-auth-strategy.md) |

## Phase 1 - Normalize CommandCenterAuth Deployment and Auth Paths

Run this alone. Do not run in parallel with any other prompt in this file.

### Prompt

You are implementing the repo-owned CommandCenterAuth deployment and auth path.

Read these files first:

- [scripts/salesforce/setup-command-center-connected-app.ps1](scripts/salesforce/setup-command-center-connected-app.ps1)
- [scripts/salesforce/data-cloud-login-web.ps1](scripts/salesforce/data-cloud-login-web.ps1)
- [scripts/salesforce/data-cloud-get-access-token.ps1](scripts/salesforce/data-cloud-get-access-token.ps1)
- [playbooks/set-up-command-center-connected-app.md](playbooks/set-up-command-center-connected-app.md)
- [notes/evaluations/salesforce-auth-strategy.md](notes/evaluations/salesforce-auth-strategy.md)
- [README.md](README.md)

Goal:

- Make the CommandCenterAuth deployment path explicit, repeatable, and easy to validate in a fresh org or a reused org.
- Preserve the separate normal-org alias and Data Cloud alias model.
- Make it obvious which auth source is being used and what is still manual in Setup.

Implementation expectations:

- Harden any missing error handling or validation in the deployment script.
- Make sure the playbook and README point to the real deployment script and explain the dependency chain.
- Keep secrets out of tracked files.
- Do not introduce a second auth model that competes with the current one.

Deliverable:

- Updated scripts or docs that make the CommandCenterAuth deployment and Data Cloud auth bootstrap reusable without ambiguity.
- A message board update that says what changed and whether the org still requires manual Setup steps.

## Phase 2 - Normalize Data Cloud Target Registration and Manifest Inputs

Run this alone. Do not run in parallel with any other prompt in this file.

### Prompt

You are implementing manifest-driven target registration and registry normalization for Data Cloud.

Read these files first:

- [scripts/salesforce/data-cloud-register-target.ps1](scripts/salesforce/data-cloud-register-target.ps1)
- [scripts/salesforce/data-cloud-register-manifest-targets.ps1](scripts/salesforce/data-cloud-register-manifest-targets.ps1)
- [scripts/salesforce/data-cloud-guided-manifest-setup.ps1](scripts/salesforce/data-cloud-guided-manifest-setup.ps1)
- [scripts/salesforce/DataCloud.Common.ps1](scripts/salesforce/DataCloud.Common.ps1)
- [notes/registries/data-cloud-targets.json](notes/registries/data-cloud-targets.json)
- [playbooks/guided-data-cloud-manifest-setup.md](playbooks/guided-data-cloud-manifest-setup.md)
- [playbooks/upload-manifest-dataset-to-data-cloud.md](playbooks/upload-manifest-dataset-to-data-cloud.md)

Goal:

- Make manifest-derived target registration deterministic for any CSV manifest, not just the Sunrun dataset.
- Keep the registry non-secret and stable.
- Make missing or stale target entries obvious before upload.

Implementation expectations:

- Improve how default target keys, source names, object names, tenant endpoints, and notes are derived or validated.
- Preserve the current alias split and local-only secret model.
- Make sure stale target rows can be corrected without manual guesswork.

Deliverable:

- Updated registry workflow and docs that clearly separate dataset configuration from org-specific setup.
- A message board update that records what is now automatic and what still depends on manual Data Cloud Setup.

## Phase 3 - Complete Schema Inference and Metadata Generation

Run this alone. Do not run in parallel with any other prompt in this file.

### Prompt

You are tightening the CSV-to-metadata generation path.

Read these files first:

- [scripts/salesforce/data-cloud-generate-schema.ps1](scripts/salesforce/data-cloud-generate-schema.ps1)
- [scripts/salesforce/data-cloud-generate-ingest-metadata.ps1](scripts/salesforce/data-cloud-generate-ingest-metadata.ps1)
- [scripts/salesforce/data-cloud-inspect-csv.ps1](scripts/salesforce/data-cloud-inspect-csv.ps1)
- [Demos/Sunrun Demo/manifest.json](Demos/Sunrun%20Demo/manifest.json)
- [salesforce/generated/sunrun_ingest_api](salesforce/generated/sunrun_ingest_api)

Goal:

- Make schema inference and manifest-based metadata generation coherent.
- Ensure the generated metadata is useful for a fresh dataset and not just the Sunrun sample.
- Keep the output understandable enough for an operator to review before deployment.

Implementation expectations:

- Decide whether schema generation should stay as a standalone helper, become part of a bootstrap path, or both.
- Preserve the current CSV sampling logic, but tighten validation for edge cases like empty files, duplicate names, and bad headers.
- Make any generated files and output paths predictable and documented.

Deliverable:

- A clearer schema-generation path and supporting docs or script improvements.
- A message board note that states whether schema inference is now part of the bootstrap path or remains an optional helper.

## Phase 4 - Automate Stream and DLO Creation from a Manifest

Run this alone. Do not run in parallel with any other prompt in this file.

### Prompt

You are finishing the manifest-driven Data Cloud provisioning path that creates or registers the live stream and DLO metadata.

Read these files first:

- [scripts/salesforce/data-cloud-create-manifest-streams.ps1](scripts/salesforce/data-cloud-create-manifest-streams.ps1)
- [scripts/salesforce/data-cloud-generate-ingest-metadata.ps1](scripts/salesforce/data-cloud-generate-ingest-metadata.ps1)
- [scripts/salesforce/DataCloud.Common.ps1](scripts/salesforce/DataCloud.Common.ps1)
- [salesforce/generated/sunrun_ingest_api](salesforce/generated/sunrun_ingest_api)
- [notes/registries/data-cloud-targets.json](notes/registries/data-cloud-targets.json)

Goal:

- Make manifest-driven connector registration, schema registration, data stream creation, and DLO artifact generation repeatable and idempotent.
- Reduce the amount of manual Data Cloud Setup required after the manifest exists.
- Keep the generated metadata aligned with the live org state.

Implementation expectations:

- Preserve the current generated-metadata model, but make it clear what is deployed to the org versus what remains local source.
- Handle already-existing connector schemas and streams without corrupting state.
- Verify active status or otherwise confirm the org has accepted the generated stream objects.

Deliverable:

- A robust manifest-to-stream/DLO path with clear success and failure states.
- A message board update that says what is now org-automated and what still needs operator intervention.

## Phase 5 - Harden Upload Execution, Retry, and Cleanup

Run this alone. Do not run in parallel with any other prompt in this file.

### Prompt

You are hardening the upload execution path and adding the cleanup behavior needed for enterprise reuse.

Read these files first:

- [scripts/salesforce/data-cloud-upload-csv.ps1](scripts/salesforce/data-cloud-upload-csv.ps1)
- [scripts/salesforce/data-cloud-upload-manifest.ps1](scripts/salesforce/data-cloud-upload-manifest.ps1)
- [scripts/salesforce/data-cloud-list-jobs.ps1](scripts/salesforce/data-cloud-list-jobs.ps1)
- [scripts/salesforce/data-cloud-get-job.ps1](scripts/salesforce/data-cloud-get-job.ps1)
- [scripts/salesforce/data-cloud-inspect-csv.ps1](scripts/salesforce/data-cloud-inspect-csv.ps1)
- [scripts/salesforce/DataCloud.Common.ps1](scripts/salesforce/DataCloud.Common.ps1)

Goal:

- Make uploads safer to rerun after partial failure or stale org state.
- Improve retry and error messages so operators can fix the actual cause instead of guessing.
- Add whatever cleanup or rollback support is realistically achievable in this repo without overengineering it.

Implementation expectations:

- Keep the bulk-job flow working for both per-table and manifest uploads.
- Improve handling of missing targets, missing object endpoints, and job failures.
- Make it obvious how to inspect failed jobs and how to recover.

Deliverable:

- A hardened upload path and any needed cleanup helpers or docs.
- A message board update that records the remaining failure modes, if any.

## Phase 6 - Add Tableau Next Semantic-Model Discovery and Preparation

Run this alone. Do not run in parallel with any other prompt in this file.

### Prompt

You are building the next phase after Data Cloud ingestion: Tableau Next semantic-model preparation.

Read these files first:

- [notes/evaluations/tableau-next-claude-skills.md](notes/evaluations/tableau-next-claude-skills.md)
- [notes/evaluations/README.md](notes/evaluations/README.md)
- [README.md](README.md)
- [skills/INDEX.md](skills/INDEX.md)

Goal:

- Turn the evaluation notes into a local, repo-native plan for semantic-model discovery, inspection, and setup.
- Keep the solution aligned with this repo's Windows-first, PowerShell-first, registry-backed model.
- Separate what can be done immediately from what still needs design and what still needs new automation.

Implementation expectations:

- Do not copy the upstream Claude workflow as-is.
- Create local scaffolding, prompts, or playbooks only where there is a clear path to implement them here.
- Make prerequisites explicit: Data Cloud ingestion must already be stable before the semantic-model layer is attempted.

Deliverable:

- A local semantic-model roadmap or starter workflow that this repo can actually own.
- A message board update that clearly states which Tableau Next pieces are still missing entirely.

## Phase 7 - Update README, Playbooks, and Skills After the Code Lands

Run this alone. Do not run in parallel with any other prompt in this file.

### Prompt

You are the documentation and workflow-curation agent.

Read these files first:

- [README.md](README.md)
- [skills/INDEX.md](skills/INDEX.md)
- [skills/RULES.md](skills/RULES.md)
- [playbooks/guided-data-cloud-manifest-setup.md](playbooks/guided-data-cloud-manifest-setup.md)
- [playbooks/set-up-command-center-connected-app.md](playbooks/set-up-command-center-connected-app.md)
- [playbooks/upload-manifest-dataset-to-data-cloud.md](playbooks/upload-manifest-dataset-to-data-cloud.md)

Goal:

- Make the newly implemented behavior discoverable.
- Update the entry points, prerequisites, and failure notes so the repo reflects reality.
- Convert any repeated successful workflow into a skill if it now qualifies.

Implementation expectations:

- Keep the docs accurate about what is automated and what remains manual.
- Add links to the real scripts, not placeholders.
- Preserve the current Windows-first and local-secret strategy.

Deliverable:

- Updated docs and, if appropriate, updated or new skills.
- A message board note that records any doc-only gaps still left open.

## Phase 8 - End-to-End Smoke Test and Evidence Capture

Run this alone. Do not run in parallel with any other prompt in this file.

### Prompt

You are the validation agent.

Goal:

- Re-run the end-to-end path in the smallest realistic scope and capture evidence that the repo now behaves like a reusable workflow rather than a one-off demo.

Read the relevant files for the phases you are validating, then test the shortest realistic path that proves:

- CommandCenterAuth deployment works.
- Data Cloud auth resolves.
- Manifest target registration works.
- Stream or DLO creation behaves as expected.
- Upload execution returns a diagnosable terminal state.

Deliverable:

- A final message board entry with what succeeded, what failed, and what remains org-specific.
- If anything still fails, note the exact file and state that caused it.

## Agent Message Board Operating Pattern

Every agent should use the same pattern when posting here:

1. Claim the phase.
2. Post the files you are editing.
3. Post blockers immediately when discovered.
4. Post the validation result at the end.
5. Leave a handoff note for the next phase.

Keep the board short and factual. It is for coordination, not discussion.